{
  "name": "btech-dev-cases",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "start": "npm run start -w backend",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
